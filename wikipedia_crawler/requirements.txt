pika==1.3.1            # For RabbitMQ client
requests==2.28.1       # For HTTP requests (e.g., fetching HTML)
redis==4.3.4           # For interacting with Redis
Flask==2.1.2           # If you use Flask in your app
pytest==7.2.1          # For running tests
gunicorn==20.1.0       # For deploying Flask app
